interface:
  display_name: "Kula Escrow"
  short_description: "Repo-specific patterns and workflows for kula-escrow"
  default_prompt: "Use the kula-escrow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true